{
  "name": "obsidian-zen-vocab-ai",
  "version": "3.1.0",
  "description": "中英双语AI词汇学习与复习插件",
  "main": "main.js",
  "scripts": {
    "build": "npx esbuild main.ts --bundle --format=cjs --external:obsidian --outfile=main.js",
    "dev": "npx esbuild main.ts --bundle --format=cjs --external:obsidian --outfile=main.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "obsidian": "^1.12.3",
    "typescript": "^5.9.3"
  }
}
